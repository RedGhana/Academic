#include <stdio.h>

int main(void) {
    double num = 100.0;
    printf("変数numに代入された値は「%f」です。\n", num);
    
    num = 56.78;
    printf("変数numは「%f」に変更されました。\n", num);
    
    num = 1.234;
    printf("変数numは「%f」に変更されました。\n", num);
    
    
    return 0;   
}