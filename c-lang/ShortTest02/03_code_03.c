#include <stdio.h>

int main(void) {
    printf("8進数%oを16進数で表現すると%x,10進数で表現すると%dです\n", 016, 016, 016);
    printf("10進数%dを16進数で表現すると%x,8進数で表現すると%oです\n", 16, 16, 16);
    printf("16進数%xを10進数で表現すると%d,8進数で表現すると%oです\n", 0x16, 0x16, 0x16);

    return 0;
}